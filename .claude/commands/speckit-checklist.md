---
description: "Generate a custom quality checklist for the current feature."
argument-hint: "Domain or focus area for the checklist"
---

Invoke the `speckit-checklist` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
