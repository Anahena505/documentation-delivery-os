---
description: "Identify underspecified areas in the current spec by asking targeted clarification questions."
argument-hint: "Optional areas to clarify in the spec"
---

Invoke the `speckit-clarify` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
