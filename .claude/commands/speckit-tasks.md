---
description: "Generate an actionable, dependency-ordered tasks.md for the feature."
argument-hint: "Optional task generation constraints"
---

Invoke the `speckit-tasks` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
