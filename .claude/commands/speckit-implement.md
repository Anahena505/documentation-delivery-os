---
description: "Execute the implementation plan by processing all tasks in tasks.md."
argument-hint: "Optional implementation guidance or task filter"
---

Invoke the `speckit-implement` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
