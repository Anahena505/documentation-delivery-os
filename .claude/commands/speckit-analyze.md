---
description: "Cross-artifact consistency and quality analysis across spec, plan, and tasks."
argument-hint: "Optional focus areas for analysis"
---

Invoke the `speckit-analyze` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
