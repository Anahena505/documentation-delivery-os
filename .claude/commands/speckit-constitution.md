---
description: "Create or update the project constitution, keeping dependent templates in sync."
argument-hint: "Principles or values for the project constitution"
---

Invoke the `speckit-constitution` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
