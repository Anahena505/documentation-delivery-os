---
description: "Execute the implementation planning workflow to generate design artifacts."
argument-hint: "Optional guidance for the planning phase"
---

Invoke the `speckit-plan` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
