---
description: "Assess the codebase against spec/plan/tasks and append remaining work as tasks."
argument-hint: "Optional convergence guidance"
---

Invoke the `speckit-converge` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
