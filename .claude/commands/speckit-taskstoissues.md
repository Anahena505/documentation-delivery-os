---
description: "Convert existing tasks into dependency-ordered GitHub issues for the feature."
argument-hint: "Optional filter or label for GitHub issues"
---

Invoke the `speckit-taskstoissues` skill (Skill tool) to perform this workflow step. Treat everything the user typed after the command as the skill input / feature description.

User input:
$ARGUMENTS
