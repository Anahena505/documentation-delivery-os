apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "d2os.fullname" . }}-config
  labels:
    {{- include "d2os.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
