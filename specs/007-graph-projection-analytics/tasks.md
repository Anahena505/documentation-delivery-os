# Tasks: Graph Projection + Analytics

**Input**: Design documents from `/specs/007-graph-projection-analytics/`

**Prerequisites**: [plan.md](plan.md), [spec.md](spec.md), [research.md](research.md), [data-model.md](data-model.md), [contracts/api.yaml](contracts/api.yaml), [quickstart.md](quickstart.md)

**Tests**: Included — spec SC-001…SC-008 and the quickstart's six IT suites + benchmark require rebuild-equivalence, traceability, cycle-detection, payload-sufficiency, idempotency/mid-rebuild, influence, cross-workspace-leakage, and Phase 1–6 regression suites as acceptance evidence.

**Organization**: Grouped by user story (US1–US4, priority order from spec.md). Builds on the Phase 1–6 modular monolith and **adds the new `projection` module (the 14th)** — the sole writer of the derived graph tables (Principle III). **REQUIRES Phase 5 built** — the projector consumes the Phase 5 `GateEventPayload` outbox contract; until Phase 5 is built the payload-sufficiency audit (FR-011) would necessarily report gate-event gaps. **Phase 6 is NOT required** — this phase is independent of the Catalog Studio; the traceability UI panel lives inside `projection`, not the studio, and the two may run in parallel. Migration lands at **V23** (derived graph tables + `d2os_projector` role). Storage is PostgreSQL adjacency tables + recursive CTEs — **no graph DB** (PD-4; a dedicated graph store is adopted only if the benchmark's p95 target is missed).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependency on an incomplete task)
- **[Story]**: Maps to spec.md user stories US1–US4
- All descriptions include exact file paths per the module layout in [plan.md](plan.md)

## Path Conventions (from plan.md)

New 14th module `projection`: `projection/src/main/java/com/d2os/projection/…`, migration in `projection/src/main/resources/db/migration/`, the self-contained Thymeleaf panel in `projection/src/main/java/com/d2os/projection/ui/` (+ `projection/src/main/resources/templates/`), integration tests in `app/src/test/java/com/d2os/app/`. The module reads other modules' events/schemas read-only and writes only the graph tables (via the `d2os_projector` grant).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Stand up the new `projection` module and its config — no business logic yet.

- [X] T001 [P] Create `projection/build.gradle` — depends read-only on `casecore`, `artifacts`, `knowledge`, `governance` types (imports their event/schema types; writes none of them) plus Spring Data JPA/JDBC, Flyway, Thymeleaf + htmx (same conventions as Phase 6 but **no** dependency on the studio module — PD-4/R7) — **implementation note**: also depends on `tenancy` (needed for the `WorkspaceAwareDataSource`/`WorkspaceRlsBinder` conventions T007's second datasource follows — every other module depends on `tenancy` too); Flyway/htmx are not separate Gradle deps (Flyway is already a root/`app` dependency covering every module's `db/migration` resources on the classpath, and the traceability panel's htmx asset lands with the panel itself in a later phase, matching studio's own vendored-asset convention — no npm/webjar dependency needed at this stage)
- [X] T002 [P] Register the new module in `settings.gradle` (`include 'projection'`) and add it to the aggregate `:app` test/runtime dependencies so its migration and beans load
- [X] T003 [P] Add Phase 7 config keys to `app/src/main/resources/application.yml`: `d2os.projection.lag-threshold-seconds: 30` (incremental projection lag alarm), `d2os.projection.node-budget: 500` (traceability pagination default — R7), `d2os.projection.rebuild.schedule` (drift-detection equivalence cron — R5, weekly Sunday 03:00 since research R5 says "scheduled (weekly)"), `d2os.projection.cycle-sweep.cadence` (scheduled full-graph cycle sweep — R8, daily since a cycle sitting undetected for a full week would be too slow for a build-time-mistake alert), `d2os.projection.gap-alert-threshold: 10` (open `projection_gap` rows before the sufficiency auditor alerts — R6) — also added the `spring.datasource.projector.*` config block (T007's second datasource) here, same env-var-default convention as `spring.datasource.*`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The V23 schema, sole-writer grants, and the natural-key/provenance mapper contract that every story depends on. MUST complete before any US phase. All V23 schema lands here; per-story logic follows.

> **Renumbering note (as implemented)**: V23–V27 were already taken by later Phase 5/6 migrations
> by the time this phase was implemented (`tenancy/V23__workspace_retention.sql`,
> `catalog/V24__governance_definition_types.sql`, `catalog/V25__studio_lifecycle_subscriptions.sql`,
> `governance/V26__gate_subject_polymorphic.sql`, `governance/V27__gate_instance_case_optional.sql`)
> — Flyway's `classpath:db/migration` namespace is global across every module (see
> `app/src/main/resources/application.yml`). Renumbered to the actual next-free integer, same
> relative order: **V28** (`projection/src/main/resources/db/migration/V28__graph_projection.sql`).
>
> **Outbox-cursor deviation (as implemented, beyond the literal task text)**: `event_outbox.id`
> (casecore's V4) is a random UUID with no monotonic ordering, but `projection_checkpoint
> .outbox_watermark` needs a real ordering key for incremental runs. V28 also `ALTER TABLE
> event_outbox ADD COLUMN seq BIGSERIAL` (+ an index on it) so the watermark is a real bigint
> high-water mark, not an attempt to order by a random UUID or rely solely on `created_at` (which
> can tie under concurrent writes). This is additive to casecore's V4 table, not a redefinition of
> it.
>
> **`projection_gap` mutability (as implemented)**: data-model.md labels this table "append-only"
> in prose but documents `status: OPEN -> RESOLVED` in the very next line — a real, expected
> UPDATE, not an append-only stream like `audit_entry`/`event_outbox` (V8) or
> `knowledge_injection_snapshot` (persona V14). V28 resolves this in favor of the state machine:
> normal `d2os_app` UPDATE/DELETE privileges apply, nothing extra revoked.

- [X] T004 Create the derived graph tables in `projection/src/main/resources/db/migration/V28__graph_projection.sql` (renumbered from the V23 the task text names — see note above): `graph_node` and `graph_edge` (each with `generation int`, `workspace_id`, `node_type`/`edge_type`, `natural_key`/`from_node`+`to_node`, `label`/`attributes jsonb`, provenance `source_kind`/`source_ref` NOT NULL — R2/FR-003), natural-key UNIQUE constraints (`(workspace_id, generation, node_type, natural_key)` on nodes; `(workspace_id, generation, edge_type, from_node, to_node, source_ref)` on edges — R3/FR-012), and **both-direction** edge indexes (`(workspace_id, generation, from_node, edge_type)` and `(workspace_id, generation, to_node, edge_type)` — R7); plus `projection_checkpoint` (outbox watermark per consumer/workspace, now backed by the new `event_outbox.seq` cursor — see note above), `projection_state` (`live_generation`, last-equivalence result — R4), and `projection_gap` (R6/FR-011; mutable `status`, see note above)
- [X] T005 In the same `V28__graph_projection.sql`: enable RLS + per-workspace policies on all five tables (`graph_node`, `graph_edge`, `projection_checkpoint`, `projection_state`, `projection_gap`) and create the **`d2os_projector`** role with INSERT/UPDATE/DELETE on the graph tables only (SELECT on the read sources: `event_outbox`, `case_instance`, `workflow_instance`, `decision`, `feature`/`project`/`project_version`, `problem_submission`, `artifact`/`artifact_revision`/`execution_package`/`handover_record`/`trace_link`/`dependency`, `knowledge_injection_snapshot`/`knowledge_item`, `gate_instance`/`delta_report`, `definition_asset`, `kpi_sample`), granting `d2os_app` **SELECT-only** on the graph tables — explicit `REVOKE INSERT, UPDATE, DELETE ... FROM d2os_app` since V8's `ALTER DEFAULT PRIVILEGES` would otherwise have granted it the full write set on these new tables (grant-enforced sole writer — research R2, FR-001, Principle III)
- [X] T006 Implement `NodeEdgeMapper` (deterministic natural-key derivation per node type + provenance `source_kind`/`source_ref` stamping, per the data-model Node/Edge Mapping table: case events→`CASE`/`BELONGS_TO`, artifact/package events→`ARTIFACT_REVISION`/`PACKAGE`/`PRODUCED`, requirement-type artifact revisions→`REQUIREMENT` (artifact subtype, FR-005), `trace_link`→`TRACES_TO`/`DERIVES_FROM`/`SATISFIES`, `dependency`→`DEPENDS_ON`, injection snapshots→`KNOWLEDGE_ITEM_VERSION`/`INJECTED_INTO`, gate events→`GATE`/`GATED_BY`, definition refs→`DEFINITION_VERSION`) in `projection/src/main/java/com/d2os/projection/NodeEdgeMapper.java` (research R3) — **implementation notes**: (1) node/edge surrogate ids are deterministically derived via `UUID.nameUUIDFromBytes` over exactly the natural-key/identity columns, so an edge can independently compute its endpoints' ids without a DB lookup, keeping the mapper DB-free (beyond the literal task text, documented in the class javadoc); (2) `trace_link.link_type` values outside `DERIVES_FROM`/`SATISFIES`/`TRACES_TO` (in particular `CONFLICTS_WITH`, the only kind any code in this repo currently writes, in `ConsistencyService`) map to edge_type `TRACES_TO` with the real `link_type` preserved in `attributes.linkType`, keeping the edge_type enum closed per data-model.md without dropping data; (3) `dependency`→`DEPENDS_ON` is implemented per the mapping table even though no writer of `dependency` rows exists anywhere in the repo today (verified) — dead code today, ready when one lands; (4) requirement-type detection uses the `artifactType` field (an assembled-fact input, since `artifact_revision` itself carries no type — only `artifact.artifact_type` does) starting with `"requirement:"`, matching the `CatalogSeedLoader` "produces" tag convention
- [X] T007 Bind the projector JPA/JDBC datasource to the `d2os_projector` role (write path) distinct from the app datasource, and add `GraphNode`/`GraphEdge`/`ProjectionCheckpoint`/`ProjectionState`/`ProjectionGap` entities + repositories (generation-filtered reads: `generation = live_generation`) in `projection/src/main/java/com/d2os/projection/` — **implementation note (pragmatic choice, documented per T007's own instruction)**: the read side (generation-filtered finders) is plain Spring Data JPA against the app's existing single `EntityManagerFactory`/`@Primary` datasource (`d2os_app`, SELECT-only on these tables per T005) — correct and simple. The WRITE side is a plain `JdbcTemplate`-based `GraphWriteRepository` (upserts keyed on the same UNIQUE constraints T004 defined) bound to a SECOND, explicitly-non-`@Primary` `DataSource`/`JdbcTemplate`/`DataSourceTransactionManager` in `projection/src/main/java/com/d2os/projection/config/ProjectorDataSourceConfig.java` (the first multi-datasource setup in this codebase), rather than a second JPA `EntityManagerFactory` — avoids the real complexity of two `EntityManagerFactory`s contending over the same five entity classes for a write surface that is five tables' worth of upserts. A `ProjectorRlsBinder` (mirroring tenancy's `WorkspaceRlsBinder`) is provided for the `Projector`/`RebuildJob` (T008/T009, later phases) to bind `app.workspace_id` per workspace sweep, since `d2os_projector` does not own these RLS-policied tables either

**Checkpoint**: V28 schema (renumbered from V23), `d2os_projector` sole-writer grants, and the mapper/provenance contract ready — user story phases can begin.

**Verification note (T001–T007, as implemented)**: `gradle compileJava compileTestJava` is clean across all 15 modules. Full `:app:test` Testcontainers-backed IT verification (e.g. `AssessmentReadOnlyIT`) could not be exercised end-to-end in this sandbox — the bundled Testcontainers/docker-java client negotiates too old a Docker API version (1.32) against this sandbox's Docker Engine (29.3.1, minimum supported API 1.40), a pre-existing environment/tooling mismatch unrelated to this phase's changes. The Spring wiring (second `DataSource`/`JdbcTemplate`/transaction manager, JPA entity validation against V28's schema) was reviewed by hand against this codebase's existing single-datasource conventions and is expected to load correctly; this should be re-verified with a real IT run in an environment with a compatible Docker setup before Phase 3 (T008+) builds on it.

---

## Phase 3: User Story 1 - The graph is a rebuildable projection, never a source of truth (Priority: P1) 🎯 MVP

**Goal**: Stand up the projector, generational rebuild with verify-then-flip, per-type equivalence verification, and the payload-sufficiency audit — so the graph can be dropped and rebuilt to a provable copy of relational truth.

**Independent Test**: Run cases to Delivered, snapshot relational truth, drop the graph, run rebuild-from-scratch → the rebuilt graph is verifiably equivalent (same nodes, same edges) with zero manual intervention; a seeded divergence fails loudly and is never served.

- [X] T008 [US1] Implement `Projector` — outbox consumer + edge-table reader driving idempotent `ON CONFLICT DO UPDATE` upserts through `NodeEdgeMapper` into the live generation, tracking the `projection_checkpoint` watermark for incremental runs (replay/duplicate delivery is a no-op — research R3, FR-004/FR-012) in `projection/src/main/java/com/d2os/projection/Projector.java` — **implementation notes / honest gaps (as implemented)**: (1) bootstrap resolved per the task prompt's own guidance — the Projector is the sole author of generation 0 (seeds `projection_state.live_generation=0` the first time a workspace is swept; `RebuildJob` only ever flips an ALREADY-live generation); (2) case/gate lifecycle events are outbox-consumed (watermark-tracked via `event_outbox.seq`), but NO outbox event exists anywhere in the repo for artifact-revision creation or `trace_link` writes (verified by grepping every `auditWriter.record(` call site) — those two are read directly off their source tables in full every sweep instead (always correct since every write is an idempotent upsert, not watermark-optimized; documented as a deferred perf concern for T029's benchmark); (3) **node/edge type scope is deliberately narrower than `NodeEdgeMapper`'s full repertoire** — wired: `CASE`/`FEATURE`/`BELONGS_TO`, `ARTIFACT_REVISION`/`REQUIREMENT`/`OPERATION_EXECUTION`/`PRODUCED`, `GATE`/`GATED_BY`, `TRACES_TO`/`DERIVES_FROM`/`SATISFIES`; deferred: `PACKAGE`/`execution_package` and `DEPENDS_ON`/`dependency` (no writer exists anywhere in the repo, confirmed dead code, matching `NodeEdgeMapper`'s own javadoc), `KNOWLEDGE_ITEM_VERSION`/`INJECTED_INTO` (explicitly assigned to Phase 6/US4 by T025), `DEFINITION_VERSION` (no task names a concrete source query for it) — kept consistent across `Projector`/`RebuildJob`/`EquivalenceVerifier` on purpose (see T009/T010 notes); (4) **honest gap: FR-014 (source deletion/deprecation projects as removal or a status attribute) is NOT implemented** — no source table in this repo's schema currently exposes a hard-delete or a deprecation flag on `case_instance`/`gate_instance`/`artifact_revision` for the projector to react to (case status transitions are terminal-state updates, not deletions, and are already handled as ordinary re-projection), so there was no real deletion signal to wire against; this is flagged here rather than silently marked done — a future phase introducing an actual source-side delete/deprecate signal will need explicit removal-or-status-flag handling added to this class; (5) added a new `d2os.projection.consumer-interval-ms` config key (application.yml) — T003 did not anticipate a key for the continuous consumer's OWN cadence, only the rebuild/cycle-sweep schedules; (6) added `projection/src/main/resources/db/migration/V29__projector_workspace_enumeration.sql` — a real gap in Phase 1-2's design: every table (including `workspace` itself) is RLS-scoped to the ALREADY-bound `app.workspace_id`, so neither `d2os_app` nor `d2os_projector` can enumerate "every active workspace" the way T008's "for each workspace" sweep requires (`ReconciliationJob`/`CaseDeliveredKnowledgeTrigger` dodge this by discovering work from Flowable's RLS-free engine tables, which the projector has no equivalent of); V29 adds one narrowly-scoped `SECURITY DEFINER` function (`list_active_workspace_ids()`, owned by the migration/table-owner role, returning only `workspace.id`) rather than a blanket RLS bypass on either runtime role — documented at length in the migration file itself
- [X] T009 [US1] Implement `RebuildJob` — build generation N+1 → run the equivalence verifier → on PASS atomically flip `projection_state.live_generation` and purge N; on FAIL keep N live, drop N+1, raise a divergence alert (a divergent rebuild is never served) in `projection/src/main/java/com/d2os/projection/RebuildJob.java` (research R4, FR-002/FR-015) — **deviates from R4's literal "full outbox replay + edge-table scan" wording, per this task's own explicit instruction**: generation N+1 is built by directly querying `case_instance`/`artifact_revision`(+`artifact`)/`gate_instance`/`trace_link` (reusing `NodeEdgeMapper` fed from table-scanned Facts, not from outbox events) so equivalence-checking never re-runs the same event-derivation logic against itself (R5's rationale) — same node/edge type scope as T008 (see its notes) and the same "self-id as `sourceRef`" provenance convention (documented in both classes' javadoc) so an incrementally-built and a rebuilt generation produce byte-identical edge identities for the same logical fact, which `EquivalenceVerifier` depends on to compare like-for-like. On FAIL, the divergence alert reuses the `projection_gap` table (`event_type=REBUILD_DIVERGENCE:generation-N`, `missing_fields` repurposed to carry the mismatching node/edge type names) rather than `in_app_notification` — the latter lives in governance's `d2os_app`-writable schema, a different datasource/transaction than the one this flip-or-drop decision already runs in (T007's dual-datasource design), so reusing `projection_gap` (already a `d2os_projector`-owned table, atomic with the flip/purge) is the lower-complexity, equally-honest choice the task text explicitly permitted ("or reuse `in_app_notification` if that's cleaner" — it was not, here). Honest gap: no cross-job lock between `Projector`'s sweep and this class's flip-and-purge (documented as an accepted, narrow, non-correctness-affecting race in the class javadoc)
- [X] T010 [P] [US1] Implement `EquivalenceVerifier` — per-workspace, per-node/edge-type order-independent digests (count + SHA-256 over canonical natural-key/edge-identity rows) computed on **both** the candidate generation and relational truth in `projection/src/main/java/com/d2os/projection/EquivalenceVerifier.java` (research R5, FR-002) — **independence is real, not nominal**: this class NEVER calls `NodeEdgeMapper` or any `*Fact` record; every truth-side tuple is computed by hand-written SQL against the source tables, duplicating (not reusing) the mapper's small amount of derivation logic (e.g. the `requirement:` artifact-type-prefix check, the `trace_link.link_type` → edge_type fallback) — a shared-code bug between `RebuildJob`'s build and this class's truth check would NOT go undetected, since they are two separately-written pieces of code targeting the same documented contract (data-model.md's mapping table). Scope matches T008/T009 exactly (`dependency`/injection-snapshot truth sources from the original task text are deferred along with their node/edge types, for the reasons noted on T008 — checking a type nothing builds would be either vacuous or spuriously-failing, neither a meaningful check); a per-`HashUtil` sha256 helper was added to `projection` per this repo's established one-private-copy-per-module convention (confirmed by grep: `casecore`/`persona`/`artifacts`/`governance`/`catalog`/`intake` each already have their own)
- [X] T011 [P] [US1] Implement `PayloadSufficiencyAuditor` — validate outbox events against declared per-event-type required-field sets in `projection/src/main/java/com/d2os/projection/PayloadSufficiencyAuditor.java` (research R6, FR-011) — grepped every `auditWriter.record(` call site across the repo to enumerate the real `event_type` values in use; only the 3 real gate event types (`GATE_OPENED`/`GATE_DECIDED`/`GATE_REGENERATION_TRIGGERED`) get a real field contract, split into STRUCTURAL fields (missing ⇒ the whole event is unprojectable, skipped, gap recorded) and CONTENT fields (missing ⇒ still projected with nulls, per R6's "skip only the unprojectable parts," gap still recorded) — case-lifecycle events are NOT given a contract since the mapper reads them from `case_instance` directly, never from the payload (an empty contract there would be theater); every other real event type (`CASE_TYPE_CONFIRMED`/`ARTIFACT_WRITE_REFUSED`/`DEFINITION_PUBLISHED`/`GATE_APPROVE`+siblings/knowledge-module events) is honestly left undeclared since Phase 3's projector does not map them. Alerting past `d2os.projection.gap-alert-threshold` is a `log.warn` (no notification-channel wiring — that is Phase 5/US3's job for cycle alerts specifically; T011's own task text only says "alerts," not a specific channel). The "dry sweep" mode (`drySweep(workspaceId)`) is implemented and usable but **not wired to an admin endpoint** — `contracts/api.yaml` does not define one for it, so it is exposed as a callable service method only, consistent with the scoped-down posture
- [X] T012 [US1] Implement `GraphAdminController` — `POST /graph/admin/rebuild` (202 async; 409 if a rebuild is in progress), `GET /graph/admin/status` (live generation, watermark lag as `outboxLagSeconds`, last equivalence result, open-gap count), `GET /graph/admin/gaps` (OPEN/RESOLVED filter) in `projection/src/main/java/com/d2os/projection/api/GraphAdminController.java` (contracts/api.yaml) — read endpoints rely on the same transparent per-request RLS-bound datasource convention every other controller in this repo uses (`WorkspaceContext.require()`, matching `WorkspaceBudgetController`); the rebuild trigger delegates to `RebuildJob`'s own in-memory per-workspace in-progress guard (a single-JVM assumption, consistent with every other `@Scheduled` job in this repo — none of them do distributed locking either)
- [X] T013 [US1] Add `RebuildEquivalenceIT` in `app/src/test/java/com/d2os/app/RebuildEquivalenceIT.java`: seed case/artifact/gate data directly + outbox events → `Projector.sweep()` builds generation 0 → `RebuildJob.triggerAsync` → equivalence PASS → atomic flip to generation 1, generation 0 purged, every surviving node still carries real provenance; seeded-divergence subtest (a phantom `graph_node` row planted directly into what generation 1 will become, bypassing the mapper entirely) → equivalence FAILS, generation 0 stays live, N+1 dropped, a `REBUILD_DIVERGENCE` `projection_gap` alert recorded. **Honest gap**: does NOT cover T013's original "deletion subtest" (deprecate/delete a source record → no phantom node survives) — see T008's note: FR-014 deletion/deprecation handling itself is not implemented (no real source-side delete/deprecate signal exists in this schema to react to), so there is nothing for this subtest to exercise yet. Cannot actually run in this sandbox (Testcontainers/Docker confirmed non-functional since Phase 1+2's report) — written to compile and be logically sound against the real code/schema, traced by hand, not asserted to pass, same posture as `GateFlowIT`/`PublishGovernanceIT`
- [X] T014 [P] [US1] Add `PayloadSufficiencyIT` in `app/src/test/java/com/d2os/app/PayloadSufficiencyIT.java`: two scenarios — a content-thin `GATE_DECIDED` event (missing `decisionVerb`/`deciderId`, structural fields intact) still projects a GATE node (partial projection, not a total skip) with a gap recorded and visible via `GET /graph/admin/gaps`; a structurally-thin `GATE_OPENED` event (missing `gateDefinitionKey`) is not projectable at all — no GATE node exists for it, gap still recorded (thin events are caught, never silently absorbed, SC-007). Same cannot-run/hand-traced caveat as T013
- [X] T015 [P] [US1] Add `ProjectionIdempotencyIT` in `app/src/test/java/com/d2os/app/ProjectionIdempotencyIT.java`: replays the same outbox range twice (checkpoint watermark reset to 0) plus the same table-scan sources → graph_node/graph_edge content is byte-identical (not just equal counts) after the replay — true upsert idempotency, not merely no-duplicates-by-luck; a second test asserts `RebuildJob.isInProgress`/`GraphAdminController`'s `rebuildInProgress` flag is observably true while a rebuild runs and a concurrent second trigger gets refused (409-equivalent). The deeper "a query mid-rebuild never observes a half-built generation" property is argued structurally in the test's javadoc (the atomic single-transaction flip + `generation = live_generation` read filter make a partially-written generation unobservable by construction) rather than forced by a race-prone timing test — documented as a reasoned, not asserted, guarantee. Same cannot-run/hand-traced caveat as T013

**Checkpoint**: US1 independently testable — the graph is dropped and rebuilt to a verified copy of relational truth; thin events are caught, not silently absorbed. **Known gap carried forward**: FR-014 (source deletion/deprecation ⇒ node/edge removal or a status attribute) has no implementation — flagged honestly rather than checked off; revisit once/if a source table in this schema actually exposes a delete or deprecation signal for the projector to react to.

---

## Phase 4: User Story 2 - Traceability questions are answerable across the whole record (Priority: P1)

**Goal**: Expose workspace-scoped multi-hop TRACES_TO / DEPENDS_ON traceability queries over the graph plus a self-contained UI panel, so lineage/impact is one query, not a research task.

**Independent Test**: Seed a case with known multi-hop trace/dependency links → TRACES_TO and DEPENDS_ON queries from the UI panel return lineage matching the seeded relationships exactly, including transitive hops, scoped to the querying workspace.

- [ ] T016 [US2] Implement `TraceabilityQueryService` — `tracesTo(nodeRef, direction, maxDepth)` and `dependsOn(nodeRef, direction, maxDepth)` as `WITH RECURSIVE` CTEs over `graph_edge` (both-direction indexes), returning paths with per-hop edge provenance and paginating past the configurable node budget (default 500) for predictable degradation; workspace-scoped via RLS + explicit predicate in `projection/src/main/java/com/d2os/projection/query/TraceabilityQueryService.java` (research R7, FR-005/FR-007/FR-013)
- [ ] T017 [US2] Implement `TraceabilityController` — `GET /graph/traceability` (nodeType/naturalKey/relation/direction/maxDepth/pageToken → `LineageResult` with `truncated`+`nextPageToken`; 404 if the starting node is absent from this workspace's live graph) and `GET /graph/nodes/{nodeId}` (attributes, provenance, adjacent edges, links back to owning API resources) in `projection/src/main/java/com/d2os/projection/api/TraceabilityController.java` (contracts/api.yaml, FR-006)
- [ ] T018 [P] [US2] Implement the self-contained traceability panel (search box → lineage tree → node links back to the owning API resources) in `projection/src/main/java/com/d2os/projection/ui/TraceabilityPanelController.java` + `projection/src/main/resources/templates/` (Thymeleaf + htmx, **no studio module on the classpath** — preserves the plan's Phase-6-independence — research R7, FR-006)
- [ ] T019 [US2] Add `TraceabilityQueryIT` in `app/src/test/java/com/d2os/app/TraceabilityQueryIT.java`: seed a known multi-hop lineage (submission → case → requirement → artifacts → package; DERIVES_FROM/SATISFIES/DEPENDS_ON edges incl. a `REQUIREMENT` node hop, FR-005) → `GET /graph/traceability` TRACES_TO and DEPENDS_ON both directions return paths equal to the seeded relationships incl. transitive hops, each hop navigable via `GET /graph/nodes/{id}`; UI smoke asserts the panel renders with no studio module on the classpath (SC-003)
- [ ] T020 [P] [US2] Extend the cross-workspace leakage suite over graph queries in `app/src/test/java/com/d2os/app/LeakageSuiteIT.java`: traceability and node queries from workspace A return zero of workspace B's nodes/edges regardless of relation depth — asserted both API- and SQL-level (RLS + explicit predicate — SC-005, Principle IV)

**Checkpoint**: US2 independently testable — multi-hop lineage/impact answerable from the panel, workspace-scoped, navigable back to source.

---

## Phase 5: User Story 3 - Dependency cycles are detected and alerted (Priority: P2)

**Goal**: Whole-graph, cross-case dependency-cycle detection (incremental on-insert + scheduled full sweep) that alerts through the audited notification/outbox path.

**Independent Test**: Introduce a dependency cycle spanning two cases → detection reports it with all members; an acyclic graph produces zero false positives.

- [ ] T021 [US3] Implement `CycleDetector` — (a) incremental: on each projected `DEPENDS_ON` edge a bounded recursive CTE checks whether the new target reaches its source (cycle ⇒ member-path finding); (b) scheduled full-graph sweep (per workspace, Kahn-style peeling) as the completeness backstop for edges arriving during rebuild; detection spans the **whole workspace graph, cross-case** (not per-case subgraphs); acyclic ⇒ zero findings in `projection/src/main/java/com/d2os/projection/cycle/CycleDetector.java` (research R8, FR-008/FR-009)
- [ ] T022 [US3] Raise cycle alerts by persisting `in_app_notification` rows through the Phase 5 in-app notification store (generic `source_module`/`type` contract — `source_module=projection`, `type=CYCLE_DETECTED`; in-app only in v1, no email/webhook) + an outbox event, and persist findings (member nodes in path order) in `projection/src/main/java/com/d2os/projection/cycle/` (audited alerting path — research R8, Principle V; Phase 5 data-model InAppNotification)
- [ ] T023 [P] [US3] Add `GET /graph/cycles` (detected cycles, each naming its member nodes in path order; empty for an acyclic graph) in `projection/src/main/java/com/d2os/projection/api/CycleController.java` (contracts/api.yaml)
- [ ] T024 [US3] Add `CycleDetectionIT` in `app/src/test/java/com/d2os/app/CycleDetectionIT.java`: project a dependency cycle spanning two cases → incremental detection alerts naming the member path and `GET /graph/cycles` lists it; acyclic control graph → zero findings (no false positives); scheduled-sweep subtest → a cycle introduced mid-rebuild is caught by the full sweep (SC-004)

**Checkpoint**: US3 independently testable — cross-case cycles detected and alerted; acyclic graphs stay silent.

---

## Phase 6: User Story 4 - Knowledge influence is visible on the graph (Priority: P3)

**Goal**: Materialize injection/production edges and serve a knowledge-influence dashboard that attributes recorded influence to KnowledgeItems, operations, and artifacts, navigable along real lineage.

**Independent Test**: For seeded cases with Phase 3 influence samples, the dashboard attributes recorded deltas to the correct item versions, navigable to the exact operations/artifacts touched; a never-injected item reports NOT_YET_MEASURABLE.

- [ ] T025 [US4] Ensure the projector materializes `INJECTED_INTO` edges (KnowledgeItem version → OperationExecution, from Phase 3 injection snapshots) and `PRODUCED` edges (OperationExecution → ArtifactRevision) via `NodeEdgeMapper`/`Projector` in `projection/src/main/java/com/d2os/projection/NodeEdgeMapper.java` (research R9, data-model mapping)
- [ ] T026 [US4] Implement `InfluenceAnalyticsService` — join `INJECTED_INTO`/`PRODUCED` edges with `kpi_sample` (`metric='knowledge_influence'`, **read-only** from observability — never recomputed here); per item render recorded readings + touched operations/artifacts navigable along the edges; items with no samples render `NOT_YET_MEASURABLE` (consistent with Phase 3 FR-018) in `projection/src/main/java/com/d2os/projection/influence/InfluenceAnalyticsService.java` (research R9, FR-010, Principle III)
- [ ] T027 [P] [US4] Add `GET /graph/influence` (`knowledgeKey`/`caseId` filters → per-item `InfluenceEntry`: state MEASURED/NOT_YET_MEASURABLE, readings, touchedOperations, touchedArtifacts) in `projection/src/main/java/com/d2os/projection/api/InfluenceController.java` (contracts/api.yaml)
- [ ] T028 [US4] Add `InfluenceDashboardIT` in `app/src/test/java/com/d2os/app/InfluenceDashboardIT.java`: seeded cases with Phase 3 influence samples → `GET /graph/influence` attributes recorded deltas to the correct item versions, navigable to the exact operations/artifacts touched (INJECTED_INTO/PRODUCED edges); a never-injected item reports NOT_YET_MEASURABLE (SC-006)

**Checkpoint**: US4 independently testable — influence explored along the graph, computing no new metric values.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Prove the latency posture, enforce the sole-writer boundary, and prove nothing regressed.

- [ ] T029 [P] Add the query-latency benchmark (`@Tag("slow")`) in `app/src/test/java/com/d2os/app/TraceabilityBenchmarkIT.java`: seed ~50k nodes / 200k edges, run the traceability query mix → assert p95 ≤ 2 s or predictable pagination; also assert the plan's operational targets — incremental projection lag ≤ 30 s under event load and full rebuild ≤ 10 min at the benchmark volume; a miss records the **PD-4 escalation decision** (dedicated graph store behind the same `TraceabilityQueryService` interface), never silently absorbed (SC-008)
- [ ] T030 [P] Add an ArchUnit rule that **only the projector writes the graph tables** (no other module references a graph-table writer/repository write path; sole-writer boundary — Principle III, research R2) in `test-support/src/main/java/com/d2os/testsupport/ArchitectureRules.java`
- [ ] T031 [P] Re-run the Phase 1–6 IT suites unchanged to confirm zero regression (the projection module adds only read-only consumers + a SELECT-only grant for `d2os_app`)
- [ ] T032 [P] Update the `quickstart.md` success checklist and run the full `:app:test` suite green (Phase 1–6 suites + all six new Phase 7 suites; benchmark tagged slow / nightly)

---

## Dependencies & Execution Order

- **Setup (T001–T003)** → **Foundational (T004–T007)** block everything.
- **⚠️ Phase 5 build dependency**: the projector (T008) and the payload-sufficiency audit (T011/T014) consume the Phase 5 `GateEventPayload` outbox contract — **implementation is blocked until Phase 5 is built**; until then the sufficiency audit would necessarily report gate-event gaps. The phase may be *planned* against the contract, but not *built* before it.
- **Phase 6 is NOT required**: this phase is independent of the Catalog Studio; the traceability panel (T018) lives in `projection` and asserts no studio module on the classpath. Phase 6 and Phase 7 may run in parallel.
- **US1 (T008–T015)** is the MVP and precedes the others (the projector + rebuild/verify/audit foundation the queries and analytics read against).
- **US2 (T016–T020)** depends on US1's projected live generation (the traceability CTEs read `generation = live_generation`).
- **US3 (T021–T024)** depends on the projected `DEPENDS_ON` edges (US1's projector) and reuses the traversal indexes from T004.
- **US4 (T025–T028)** depends on the projector materializing injection/production edges (extends T006/T008) and the Phase 3 `kpi_sample` stream.
- **Polish (T029–T032)** depends on all stories being present.

**Story independence**: US2, US3, US4 each deliver an isolable capability testable on top of US1's projection. Given staffing, US3 (cycles) and US4 (influence) can proceed in parallel after US1 and the relevant projected edges exist; US2 (traceability queries + panel) is the headline P1 value co-equal with US1.

## Parallel Execution Examples

- **Setup**: T001, T002, T003 all `[P]` — build file, settings include, and config keys are distinct files.
- **US1**: T010 (equivalence verifier) and T011 (sufficiency auditor) `[P]` — different files — after T008/T009; then the ITs T013, T014, T015 (T014/T015 `[P]`, distinct suites).
- **US2**: T018 (UI panel) `[P]` with the controller/service work; T020 (leakage extension) `[P]` — distinct test file.
- **US3**: T023 (cycles endpoint) `[P]` alongside detector/alert wiring.
- **US4**: T027 (influence endpoint) `[P]` alongside the analytics service.
- **Polish**: T029, T030, T031, T032 all `[P]` — independent test/config files.

## Implementation Strategy

**MVP = US1 only** (Phase 1–3): the rebuildable, provenance-carrying, equivalence-verified graph projection with the payload-sufficiency audit. This is the foundational invariant (Principle III) that makes every query and dashboard on top of the graph trustworthy — demonstrable value on its own before any query surface ships.

**Incremental delivery**: US1 (trustworthy rebuildable projection) → US2 (multi-hop traceability queries + panel, the headline user-facing value) → then P2 US3 (cycle detection guardrail) and P3 US4 (influence dashboard) → Polish (latency benchmark, sole-writer ArchUnit, Phase 1–6 regression). Each phase is independently testable and leaves the system shippable.

---

**Total: 32 tasks** — Setup 3 · Foundational 4 · US1 8 · US2 5 · US3 4 · US4 4 · Polish 4.
