# Java / Gradle
target/
build/
.gradle/
*.class
*.jar
*.war
bin/
out/
# Keep the Gradle wrapper jar (negation must come AFTER *.jar to win)
!gradle/wrapper/gradle-wrapper.jar

# Redundant git-bundle export — not source
*.bundle

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Env / secrets
.env
.env.*
!.env.example
*.key
*.pem

# OS
.DS_Store
Thumbs.db
*.swp
*.tmp

# Agent folders (may contain credentials — see spec-kit setup note)
.claude/settings.local.json

# Test / build output
coverage/
*.log

# Independently-versioned sibling repo (Phase 0 template library) — not part of this repo
d2os-platform/
